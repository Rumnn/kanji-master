render: https://render.com

# Render Blueprint - Kanji Master
# Deploy cả Frontend + Backend + Socket.IO trên 1 service

services:
  - type: web
    name: kanji-master
    runtime: node
    plan: free
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: mongodb+srv://rum_db_user:<Ntd158683>@cluster0.uqqhwbg.mongodb.net/?appName=Cluster0
        sync: false  # Set manually in Render dashboard
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
